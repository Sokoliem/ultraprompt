---
description: Check status of the launchd worktree monitor.
---

Show launchd monitor status.

Run: `bash ${CLAUDE_PLUGIN_ROOT}/scripts/install-launchd.sh status`

Shows: launchctl load state, plist file presence, last scan output, recent stderr.
