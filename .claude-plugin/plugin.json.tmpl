{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "ultraprompt",
  "version": "${version}",
  "description": "Ultraprompt V${version_mm} cognitive control-plane plugin for Claude Code and Codex: ${catalog.skills} skills, ${catalog.agents} agents, ${catalog.mcp_tools} MCP tools, ${catalog.commands} commands, typed local memory, safe dream jobs, governed learning, workflow pathfinding, capability graph, live Mission Control dashboard, evidence-led release gates, dual-runtime install. V${version_mm} hardens the safety and integrity surface: every PreToolUse and Stop hook is structurally fail-open (a malformed payload can never brick a tool call); a catalog-count drift guard asserts every count site against the files on disk; and the install guide renders from one template so it cannot go stale — building on V9.2's single-VERSION release-integrity pass.",
  "author": {
    "name": "Ultraprompt Toolkit"
  },
  "license": "MIT",
  "outputStyles": "./output-styles/",
  "mcpServers": "./.mcp.json",
  "keywords": [
    "claude-code",
    "codex",
    "mcp-server",
    "hooks",
    "agentic-ai",
    "code-review",
    "security",
    "observability"
  ]
}
